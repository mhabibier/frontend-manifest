# k8s/base/dashboard-service/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dashboard-service
  namespace: dashboard-service
  labels:
    app.kubernetes.io/name: dashboard-microservice
    app.kubernetes.io/component: server
    app.kubernetes.io/version: "1.0.0"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: dashboard-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1 # Menjalankan 1 pod ekstra saat update
      maxUnavailable: 0 # Menjamin ketersediaan 100% (Zero Downtime)
  template:
    metadata:
      labels:
        app: dashboard-service
        app.kubernetes.io/name: dashboard-microservice
        app.kubernetes.io/component: server
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "3000"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: dashboard-service-sa
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        runAsGroup: 1001
        fsGroup: 1001
        seccompProfile:
          type: RuntimeDefault
      terminationGracePeriodSeconds: 30
      imagePullSecrets:
        - name: registry-credentials
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app: dashboard-service
                topologyKey: kubernetes.io/hostname
      containers:
        - name: dashboard-service
          image: registry.yourdomain.com/dashboard-microservice:1.0.0
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 3000
              protocol: TCP
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 1001
            capabilities:
              drop: ["ALL"]
          envFrom:
            - configMapRef:
                name: dashboard-service-config
          env:
            - name: API_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: dashboard-service-secret
                  key: API_CLIENT_SECRET
          startupProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 10
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
